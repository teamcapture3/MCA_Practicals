SET SERVEROUTPUT ON;

// Q1 //
CREATE TABLE CUSTOMER (
CID VARCHAR2 (10) NOT NULL,
CNAME VARCHAR2 (10) NOT NULL,
ADDRESS VARCHAR2 (20) NOT NULL
)
PARTITION BY HASH (CID)
(
PARTITION CU1,
PARTITION CU2
);

DESC CUSTOMER;

// Q2 //
ALTER TABLE CUSTOMER ADD PARTITION CU3;
SELECT TABLE_NAME,PARTITION_NAME FROM USER_TAB_PARTITIONS WHERE TABLE_NAME='CUSTOMER';

// Q3 //
INSERT INTO CUSTOMER VALUES ('C31', 'NINAD', 'CHEMBUR');
INSERT INTO CUSTOMER VALUES ('C32', 'ANURAG', 'KURLA');
INSERT INTO CUSTOMER VALUES ('C33', 'JAY', 'SION');
SELECT * FROM CUSTOMER;

// Q4 //
SELECT * FROM CUSTOMER PARTITION (CU1);
SELECT * FROM CUSTOMER PARTITION (CU2);
SELECT * FROM CUSTOMER PARTITION (CU3);

// Q5 //
ALTER TABLE CUSTOMER MERGE PARTITIONS CU1,CU2 INTO PARTITION CU4;
